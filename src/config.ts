// Configuration file for LocalHub app

// API Base URL - Update this with your actual backend URL
export const API_BASE_URL = 'https://localhub-backend-so0i.onrender.com/api';

// App settings
export const APP_CONFIG = {
  // Maximum number of images per post
  maxImagesPerPost: 5,
  
  // Default number of posts to load per page
  defaultPageSize: 10
}; 